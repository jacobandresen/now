AdministrationPanel = Ext.extend(AdministrationPanelUi, {
    initComponent: function() {
     

      //TODO: login
      //TODO: handle choose collection
      AdministrationPanel.superclass.initComponent.call(this);
    }
});
