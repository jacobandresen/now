describe("JobWindow", function() {

  it("should list details about the currrently running job for the user", function () {
    expect(true).toBeFalsy();
  });

  it("should be able to stop the job for the user", function () {
    expect(true).toBeFalsy();
  });

  it("should be able to schedule a job for the user", function () {
    expect(true).toBeFalsy();
  });

});
